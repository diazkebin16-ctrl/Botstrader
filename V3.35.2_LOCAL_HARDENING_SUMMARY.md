# BotsTrader V3.35.2 — Local Hardening Summary

## Applied locally
- Fixed OANDA dependent-stop replacement so `stopLoss` is sent in the JSON request body (`body=body`).
- Trade-management R calculations use broker fill price when available.
- Added persistent management-write failure observability.
- New-entry blackout windows (America/New_York): 07:00–10:00 ET and 15:00–19:00 ET.
- Existing managed trades continue normal management during blackout windows.
- Daily cutoff: managed open trades are flattened during the weekday 16:50–19:00 ET rollover window; the cutoff stops at 19:00 so legitimate evening trades are not immediately closed.
- Event-loop hardening: synchronous outcome resolvers are offloaded with `asyncio.to_thread`.
- Event-loop hardening: synchronous research refreshes, strategy-health evaluation and evidence-gated model refresh work inside `scan()` are also offloaded from the asyncio thread.
- Event-loop hardening: periodic System Evaluation, Governance, Smart Execution observability and Ensemble observability are offloaded from the asyncio loop.

## Local security validation
- Security Manager unit/integration tests passed.
- Simulated startup sequence with schema initialization + dummy PAPER credentials returned `SECURITY_READY` with all checks true.
- Therefore no source-code security defect was identified locally. A Railway-only `SECURITY_STARTUP_FAILED` still requires runtime evidence if it reappears after deployment (env/secrets/persistent DB/runtime manifest).

## Historical execution / indicator audit status
- Current audit DB contains 9,062 signals, 8,052 learning samples and 32,208 shadow trials, but only 2 broker-evidenced executions in `signals`, `decision_log`, `execution_audit`, `trade_memory`, and related order/fill evidence.
- Because the older executed population is not present in the available local DBs, the intended historical executed-trade Indicator Discrimination Audit cannot be certified from this package alone.
- No indicator/filter strategy changes were made from the broader shadow/counterfactual population.

## Validation
- Full local regression will be re-run after final local hardening; see the latest validation result below.
- Four FastAPI `on_event` deprecation warnings remain; they are pre-existing warnings, not test failures.

## Final local hardening pass
- Found and fixed a schedule interaction bug: a cutoff defined as "16:50 onward" would also close legitimate trades opened after the 19:00 ET evening reopening. The flattening authority is now restricted to weekdays 16:50–19:00 ET.
- Added boundary coverage for 16:49, 16:50, 18:59, 19:00 ET and Sunday market reopen behavior.
- Added an actual-fill integration regression: BE activation is governed by the broker fill, not the planned entry.
- Added symmetric BUY/SELL coverage for profit-lock and trailing behavior.
- Added a static regression guard requiring direct POST/PUT/PATCH `req()` calls to use the `body=` keyword.
- Extended event-loop hardening to synchronous research refreshes, strategy-health evaluation, AI-director outcome reconciliation and evidence-gated model training work inside `scan()`.
- Added `.gitignore` and `PROJECT_STATUS.md` for safe future public-GitHub synchronization and cross-chat continuity.
- Local static credential scan found no non-test hardcoded secret-like literal assignments.
- Python bytecode compilation passed.

## Latest validation
- Full regression: **226 passed, 0 failed**.
- `python -m compileall`: **PASS**.
- Remaining warnings: 4 pre-existing FastAPI `on_event` deprecation warnings.
